import 'package:flutter/material.dart';

class CreateSerialScreen extends StatelessWidget {
  const CreateSerialScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
